# Bird Detection Edge Capture Agent - Python Dependencies
# Requirements for Raspberry Pi

# Core dependencies
numpy==1.26.4
PyYAML>=6.0
requests>=2.28.0

# Computer Vision
# Use opencv-python-headless for headless systems (no GUI dependencies)
# For systems with display, use opencv-python instead
opencv-python-headless==4.12.0.88

# Flask for metrics HTTP server
Flask>=2.3.0
Werkzeug>=2.3.0

# Raspberry Pi Camera
# Note: picamera2 is typically installed via apt on Raspberry Pi OS:
#   sudo apt install -y python3-picamera2
# If installing via pip (not recommended on RPi OS):
picamera2>=0.3.0

# Optional: For better production deployments
# gunicorn>=20.1.0  # Production WSGI server for Flask
